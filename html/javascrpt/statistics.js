function getVisits(value) {
    document.getElementById("results").innerHTML = "There are " + value + " visits to the site.";
}

function getTable(value) {
    document.getElementById("results").innerHTML = "There are " + value + " searchs for table.";
}

function getLine(value) {
    document.getElementById("results").innerHTML = "There are " + value + " searchs for line.";
}

function getBar(value) {
    document.getElementById("results").innerHTML = "There are " + value + " searchs for bar.";
}